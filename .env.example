PORT=3000
API_KEY=replace-with-internal-api-key
OPENAI_API_KEY=replace-with-openai-api-key

# In production on Vercel, set API_KEY and OPENAI_API_KEY in the Project
# dashboard (Settings → Environment Variables) for Production and Preview.
# PORT is unused on Vercel (only relevant for the local Fastify dev server).
